{
  "name": "drupal-cms-ai-builder",
  "private": true,
  "workspaces": [
    "packages/*",
    "platform-app"
  ],
  "scripts": {
    "test:adapters": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
